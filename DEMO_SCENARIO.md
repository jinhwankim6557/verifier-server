# Claude Code 라이브 시연 시나리오

## 시연 개요

| 항목 | 내용 |
|------|------|
| 대상 | 개발자 (Claude Code 미경험, AI 채팅은 사용 중) |
| 시간 | 30분 |
| 환경 | 프로젝터 + 터미널 (Claude Code) + 브라우저 |
| 목표 | "Claude Code가 코드베이스를 이해하고 실용적 결과물을 만든다"는 것을 체감시키기 |

---

## 시연 흐름

### Phase 1: 도입 (0~3분)

**목표**: Claude Code가 뭔지, 일반 AI 채팅과 뭐가 다른지 30초 안에 전달

**행동**:
1. 터미널에서 `claude` 실행
2. CLAUDE.md 파일을 열어서 보여주며 설명

**멘트 예시**:
> "일반 AI 채팅은 코드를 복사해서 붙여넣어야 하지만, Claude Code는 프로젝트 안에서 직접 실행됩니다. 이 CLAUDE.md 파일을 통해 프로젝트 구조와 컨텍스트를 미리 알려줄 수 있고, 필요하면 소스코드를 직접 읽습니다."

---

### Phase 2: 기존 결과물 소개 (3~5분)

**목표**: "이런 게 나온다"는 완성 이미지를 먼저 보여줘서 기대감 형성

**행동**:
1. `aos-wallet-mock.html`을 브라우저에서 열기
2. 폰 프레임, 진입경로 전환, 플로우 가이드 패널 등을 클릭하며 보여주기

**멘트 예시**:
> "이 모바일 앱 목업은 Claude Code에게 기존 코드를 읽히고, 자연어로 요청해서 만든 겁니다. 단일 HTML 파일이고, 외부 라이브러리 없이 CSS + JS만으로 되어있어요. 이제 이걸 라이브로 만들어보겠습니다."

---

### Phase 3: 라이브 - 프롬프트 입력 (5~8분)

**목표**: 실제로 명령을 내리는 모습을 보여주고, 백그라운드 작업 시작

**행동**:
1. "지금부터 새로운 화면을 하나 만들어보겠습니다" 선언
2. 준비된 프롬프트를 터미널에 입력 (아래 프롬프트 섹션 참조)
3. Claude Code가 파일을 읽기 시작하는 모습을 잠시 보여줌
4. "작업이 진행되는 동안, 어떻게 이런 결과가 나오는지 설명하겠습니다"로 전환

**포인트**:
- 프롬프트를 직접 타이핑하지 말고 **복붙** (타이핑 시간 낭비 방지)
- Claude Code가 `Read`, `Grep` 등으로 파일을 탐색하는 초반 로그를 잠시 보여주면 "코드를 직접 읽는다"는 걸 체감시킬 수 있음

---

### Phase 4: 설명 타임 (8~18분)

**목표**: 작업이 돌아가는 동안, AI-Mockup-Guide.md 기반으로 프로세스와 팁 설명

**설명 순서**:

#### 4-1. 기존 프로세스 vs AI 활용 프로세스 (3분)
- AI-Mockup-Guide.md 섹션 3의 플로우 다이어그램 활용
- 핵심: "PPT 반복 루프가 사라진다"

#### 4-2. 프롬프트 잘 쓰는 법 - 3요소 (3분)
- **맥락**: 왜 만드는지
- **참조**: 어떤 코드를 기반으로
- **요구**: 어떤 형태로
- 방금 입력한 프롬프트를 예시로 설명

#### 4-3. 실무 팁 (4분)
- 첫 결과는 70~80%, 피드백 2~3회면 90% 이상
- 수정 요청은 구체적으로 ("이쁘게 해줘" X → "테이블 헤더를 파란색으로" O)
- 파일 수정/생성 시 권한 확인이 뜸 → 안전장치
- CLAUDE.md로 프로젝트 컨텍스트를 관리하면 품질이 올라감

---

### Phase 5: 결과 확인 (18~22분)

**목표**: 방금 만들어진 결과물을 열어서 "실제로 된다"는 임팩트

**행동**:
1. Claude Code 작업 완료 확인
2. 생성된 HTML 파일을 브라우저에서 열기
3. 화면을 클릭하며 기능 보여주기

**멘트 예시**:
> "방금 만들어진 겁니다. 제가 프롬프트 하나 넣고 설명하는 동안 완성됐죠."

**만약 작업이 아직 안 끝났다면**:
> "AI가 꼼꼼하게 작업 중이네요. 진행 상황을 잠시 같이 보겠습니다."
> (Claude Code가 파일을 쓰는 과정을 실시간으로 보여주는 것도 나쁘지 않음)

**만약 결과가 이상하다면**:
> "매번 조금씩 다른 결과가 나오는데, 이럴 때 수정 요청을 합니다."
> → 자연스럽게 Phase 6으로 연결

---

### Phase 6: 라이브 수정 (22~25분)

**목표**: 피드백 → 즉시 반영 루프를 보여줘서 "회의 중 실시간 수정" 가능성 체감

**수정 요청 후보** (간단하고 실패 확률 낮은 것들):
- "통계 카드에 이모지 아이콘 추가해줘"
- "테이블 헤더 배경색을 남색(#2F325E)으로 바꿔줘"
- "차트 옆에 범례 텍스트 넣어줘"
- "페이지 타이틀을 'Verification Dashboard'로 바꿔줘"

**행동**:
1. 수정 요청 입력 (짧은 한 줄)
2. 10~30초 내 수정 완료
3. 브라우저 새로고침으로 확인

---

### Phase 7: Q&A (25~30분)

**예상 질문과 답변**:

| 질문 | 답변 요지 |
|------|----------|
| 비용은? | Pro 플랜 월 $20, Max 플랜 월 $100/$200 (사용량에 따라) |
| 보안은? | 코드가 Anthropic 서버로 전송됨. 사내 보안 정책 확인 필요 |
| 실제 코드도 짜주나? | 가능. 테스트 코드, API 구현, 리팩토링 등 다양하게 활용 |
| Copilot과 차이? | Copilot은 에디터 내 자동완성 중심, Claude Code는 프로젝트 전체를 이해하고 멀티파일 작업 가능 |
| 얼마나 정확한가? | 100%는 아님. 반드시 리뷰 필요. 도구지 대체재가 아님 |

---

## 시연용 프롬프트

### 메인 프롬프트 (Phase 3에서 사용)

```
이 프로젝트의 Verifier 관리 콘솔에 "검증 이력 대시보드" 화면 목업을 만들어줘.

먼저 이 파일들을 읽어서 데이터 구조와 기존 스타일을 파악해:
- source/did-verifier-server/src/main/java/org/omnione/did/base/db/entity/ 하위 엔티티들
- source/did-verifier-admin/frontend/src/ 하위 라우팅 구조
- admin-oid4vp-mockup.html (기존 관리 콘솔 목업 - 스타일 참고)

단일 HTML 파일(demo-dashboard-mock.html)로 만들어줘:
1. 상단 통계 카드 4개 (오늘 검증 수, 성공률, 이번주 총 건수, 활성 Policy 수)
2. 최근 검증 이력 테이블 (시간, Policy명, 프로토콜 배지, 요청자 DID, 결과 상태)
3. 프로토콜별 비율 차트 (CSS로 구현, DID VP / OID4VP / mDL)
4. 좌측에 기존 관리 콘솔 스타일의 다크 사이드바

스타일은 admin-oid4vp-mockup.html과 통일하고,
테이블 데이터는 10건 정도 Mock 데이터로 채워줘.
```

### 수정 요청 프롬프트 (Phase 6에서 사용)

**옵션 A** (색상 변경 - 가장 안전):
```
방금 만든 대시보드에서 통계 카드 상단에 이모지 아이콘을 각각 추가해줘.
검증 수는 📊, 성공률은 ✅, 이번주는 📅, 활성 Policy는 ⚙️
```

**옵션 B** (데이터 추가):
```
테이블에 "상세보기" 버튼 컬럼을 추가하고,
클릭하면 해당 행의 상세 정보를 모달로 보여줘.
```

**옵션 C** (레이아웃 변경):
```
차트 영역을 테이블 오른쪽이 아니라 통계 카드 아래로 옮기고,
가로 막대 차트로 변경해줘.
```

---

## 테스트 방법

### 목적

시연용 프롬프트의 **결과 편차를 줄이고**, 당일 리스크를 최소화한다.

### 테스트 절차

#### 1회차: 프롬프트 검증 (결과물 품질)

```
[ ] 메인 프롬프트 입력
[ ] 생성된 HTML 브라우저에서 열기
[ ] 체크리스트 확인:
    [ ] 통계 카드 4개 렌더링되는가
    [ ] 테이블에 Mock 데이터가 있는가
    [ ] 차트가 표시되는가
    [ ] 사이드바가 다크 스타일인가
    [ ] 모바일이 아닌 데스크톱 레이아웃인가
[ ] 소요 시간 기록: ___분
[ ] 전체 완성도 (주관): ___/10
```

#### 2회차: 동일 프롬프트 재실행 (편차 확인)

```
[ ] 기존 생성 파일 삭제 후 동일 프롬프트 재입력
[ ] 1회차와 비교:
    [ ] 레이아웃 구조가 유사한가
    [ ] 누락된 섹션이 있는가
    [ ] 스타일 편차가 크지 않은가
[ ] 소요 시간 기록: ___분
[ ] 편차 수준: 낮음 / 보통 / 높음
```

#### 3회차: 수정 요청 테스트

```
[ ] 2회차 결과물에 수정 옵션 A 적용
    [ ] 정상 반영되는가
    [ ] 소요 시간: ___초
[ ] 수정 옵션 B 적용
    [ ] 정상 반영되는가
    [ ] 소요 시간: ___초
[ ] 수정 옵션 C 적용
    [ ] 정상 반영되는가
    [ ] 소요 시간: ___초
```

#### 판단 기준

| 상황 | 조치 |
|------|------|
| 3회 중 2회 이상 유사한 결과 | 프롬프트 확정 |
| 매번 구조가 크게 다름 | 프롬프트를 더 구체적으로 수정 (레이아웃 명시 등) |
| 특정 섹션이 자주 누락됨 | 해당 섹션 요구사항을 프롬프트에 강조 |
| 생성 시간이 10분 초과 | 요구사항 줄이기 (차트 제거 등) |

### 프롬프트 튜닝 팁

편차가 클 경우 아래 방법으로 프롬프트를 보강:

1. **레이아웃 명시**: "2컬럼 그리드, 좌측 사이드바 240px, 우측 메인 영역"
2. **색상 지정**: "카드 배경 #f8f9fa, 사이드바 #1a1a2e"
3. **참조 강화**: "admin-oid4vp-mockup.html의 사이드바와 동일한 HTML 구조 사용"
4. **구조 순서 명시**: "상단→카드, 중단→차트+테이블 2컬럼, 하단→없음"

---

## 체크리스트 (시연 당일)

### 사전 준비
- [ ] Claude Code 로그인 상태 확인
- [ ] 프롬프트 텍스트 복사 준비 (메모장 or 클립보드 매니저)
- [ ] 백업 결과물 HTML 준비 (테스트 때 가장 잘 나온 것)
- [ ] 브라우저 탭 정리 (aos-wallet-mock.html 미리 열어두기)
- [ ] 프로젝터 해상도에서 터미널 폰트 크기 확인 (충분히 큰지)
- [ ] AI-Mockup-Guide.md를 설명용으로 열어두기

### 시연 직전
- [ ] 터미널 히스토리 클리어 (`clear`)
- [ ] Claude Code 새 세션 시작
- [ ] 불필요한 알림 끄기 (Slack, 메일 등)
